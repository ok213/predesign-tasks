интерфейс к сервису добавить забыл

User user = new User(req.getParameter("login"),
               req.getParameter("password"),
               req.getParameter("name"));
       new UserService().delete(user);
до сих пор удаляешь целиком юзера
когда можно тупо по id

удаление можно сразу в таблицу юзеров запихать
и апдейт
последней колонкой
и оттуда же черпать id сразу



сделай делит гетом
передавай в строке id
и удаляй по нему

создать отдельно
обновить/удалить можно вместе в таблице


и когда переходишь на update -заполнять инпуты старыми данными